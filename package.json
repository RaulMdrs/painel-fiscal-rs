{
  "name": "painel-fiscal-rs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "test": "vitest run",
    "test:integration": "SICONFI_INTEGRATION=1 vitest run --config vitest.integration.config.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p packages/web",
    "conferir": "tsx scripts/conferir.ts",
    "ingest": "tsx packages/ingestion/src/ingest.ts",
    "api": "tsx packages/api/src/server.ts"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
